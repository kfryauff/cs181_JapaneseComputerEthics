<!DOCTYPE html>
<html>
  <head>
    <title>Project</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/bootstrap-theme.min.css" rel="stylesheet">
    <link href="/css/custom.css" rel="stylesheet">

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
	<!-- this is a comment in HTML -->
	<div class="container">
		<div class="jumbotron">
  			<h1>{{project.title}}</h1>
  			<p>{{project.subtitle}}</p>
		</div>

    <!-- Image -->
    <div class="col-md-4" id="{{project.id}}">
      <div class="thumbnail">
          <img src="/images/{{project.image}}" alt="Lorem Pixel image" class="">
      </div>
    </div>

    
		{{{project.summary}}}

    <p><a href="/">Back &raquo;</a></p>

    <div class="bottom-menu">
    {{#each projects}}
        <div class="project col-md-3" id="{{id}}">
          <div class="thumbnail">
            <a href="/project/{{id}}">
              <img src="/images/{{image}}" alt="Lorem Pixel image" class="img-responsive">
              <h4>{{title}}</h4>
            </a>
            <!-- <div class="likeCtr"><button class="btn btn-default likeBtn">Like</button></div>
            <div class="details"></div> -->
          </div>
        </div>
    {{/each}}

    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://code.jquery.com/jquery.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="/js/bootstrap.min.js"></script>

  </body>
</html>
